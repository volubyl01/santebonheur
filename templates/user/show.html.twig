{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}

    <div class="container mt-5 ms-auto">
        <div class="d-flex flex-row-reverse">
            <a class="btn btn-secondary mb-4" href="{{ path('app_user_index') }}">Retour à la liste</a>
        </div>

        <div class="table-responsive">
            <div class="card">
                <div class="card-header bg-info text-white">
                    <h2>Utilisateur</h2>
                </div>
            </div>
            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ user.id }}</td>
                    </tr>
                    <tr>
                        <th>Email</th>
                        <td>{{ user.email }}</td>
                    </tr>
                    <tr>
                        <th>Roles</th>
                        <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
                    </tr>
                </tbody>
            </table>
            <a class="btn btn-primary text-white" href="{{ path('app_user_edit', {'id': user.id}) }}">Modifier</a>
        </div>
        <div class="d-flex flex-row-reverse mt-5">
            {{ include('user/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
